# Custom NixOS modules
#
# This directory is reserved for custom NixOS modules that extend
# the base NixOS module system. Currently empty, but can be used
# to define reusable NixOS modules that aren't part of the standard
# NixOS module tree.
#
# Example usage:
# {
#   my-custom-module = import ./my-custom-module.nix;
# }
{}
