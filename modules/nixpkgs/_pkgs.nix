# Custom packages
#
# Reserved for custom packages that aren't available in nixpkgs or need
# custom modifications beyond what overlays provide.
#
# Exposed via overlays.additions, available as `pkgs.<name>` in all configurations.
_: { }
